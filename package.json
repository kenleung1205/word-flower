{
  "name": "word-flower",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute word-flower-db --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute word-flower-db --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
